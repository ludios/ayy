{
  "name": "ayy",
  "version": "0.1.0",
  "description": "Assert module for io.js that 1) appends the provided message to the assertion message, instead of replacing it 2) if message is a function, calls it to get the message 3) A.eq does ===; no support for == or !=",
  "homepage": "https://github.com/ludios/ayy",
  "issues": "https://github.com/ludios/ayy/issues",
  "keywords": [
    "assert",
    "assertion"
  ],
  "main": "index.js",
  "scripts": {
    "test": "iojs --strong-mode --harmony-rest-parameters --harmony-arrow-functions ./node_modules/.bin/_mocha tests/*.js && ./node_modules/.bin/eslint .",
    "test-strong-mode": "iojs --strong-mode --harmony-rest-parameters --harmony-arrow-functions ./node_modules/.bin/_mocha tests/*.js"
  },
  "devDependencies": {
    "co": "^4.5.4",
    "eslint": "^0.21.2",
    "mocha": "^2.2.5"
  },
  "author": "Ivan Kozik",
  "license": "ISC",
  "repository": "ludios/ayy",
  "engines": {
    "iojs": ">= 2.0.0"
  }
}
