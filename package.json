{
	"name": "ayy",
	"version": "4.1.0",
	"description": "Assert module for node that 1) appends the provided message to the default message, instead of replacing it 2) supports calling a function to get the message 3) A.eq does ===; no support for == or !=",
	"homepage": "https://github.com/ludios/ayy",
	"issues": "https://github.com/ludios/ayy/issues",
	"keywords": [
		"assert",
		"assertion"
	],
	"main": "index.js",
	"types": "index.d.ts",
	"type": "module",
	"scripts": {
		"build": "./node_modules/.bin/tsc",
		"test": "./node_modules/.bin/ava tests/*.ts && ./node_modules/.bin/tsc --ignoreConfig --types node --noEmit && biome lint"
	},
	"devDependencies": {
		"@types/node": "^26.0.1",
		"ava": "^8.0.1",
		"typescript": "^6.0.3"
	},
	"ava": {
		"nodeArguments": [
			"--experimental-strip-types",
			"--no-warnings=ExperimentalWarning"
		]
	},
	"author": "Ivan Kozik",
	"license": "ISC",
	"repository": "ludios/ayy",
	"engines": {
		"node": ">= 14"
	}
}
