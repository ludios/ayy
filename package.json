{
  "name": "ayy",
  "version": "0.2.3",
  "description": "Assert module for io.js that 1) appends the provided message to the default message, instead of replacing it 2) supports calling a function to get the message 3) A.eq does ===; no support for == or !=",
  "homepage": "https://github.com/ludios/ayy",
  "issues": "https://github.com/ludios/ayy/issues",
  "keywords": [
    "assert",
    "assertion"
  ],
  "main": "index.js",
  "scripts": {
    "test": "node --harmony-rest-parameters --harmony-arrow-functions ./node_modules/.bin/_mocha tests/*.js && ./node_modules/.bin/eslint .",
    "test-strong-mode": "node --strong-mode --harmony-rest-parameters --harmony-arrow-functions ./node_modules/.bin/_mocha tests/*.js"
  },
  "devDependencies": {
    "co": "^4.6.0",
    "eslint": "^1.0.0",
    "mocha": "^2.2.5"
  },
  "author": "Ivan Kozik",
  "license": "ISC",
  "repository": "ludios/ayy",
  "engines": {
    "iojs": ">= 2.0.0",
    "node": ">= 4.0.0"
  }
}
